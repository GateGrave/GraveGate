Place terrain mask images here.

Recommended workflow:

- Keep the mask image at the same pixel dimensions as the map image.
- Paint whole tiles with flat solid colors in MS Paint.
- Do not use gradients, shadows, or anti-aliased brushes.
- Use the `mspaint_basic` palette documented in `apps/map-system/README.md`.
